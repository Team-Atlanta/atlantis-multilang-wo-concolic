[project]
name = "crs-multilang"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "coloredlogs",
    "python-on-whales",
    "pyinotify",
    "graphviz",
    "line-profiler",
    "redis",
    "libclang==14.0.6",
    "pyyaml",
    "loguru",
    "psutil",
]
